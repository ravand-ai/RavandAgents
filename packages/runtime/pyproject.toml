[project]
name = "ravand-runtime"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "ravand-policy",
    "ravand-profile",
    "ravand-permissions",
    "ravand-sessions",
    "ravand-audit",
]

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
